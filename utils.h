#ifndef UTILS_H
#define UTILS_H

// Current timestamp, e.g. (2025-05-06 10:05:12)
int current_timestamp(char *buffer, size_t buffer_size);

// Current date, e.g. (2025-05-06)
int current_date(char *buffer);

#endif
